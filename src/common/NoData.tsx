import * as React from 'react';
import MessageIcon from '@mui/icons-material/Message';

const TokIconButton: React.FC = () => {
    return (
        <div>
            No one registered
        </div>
    )
}

export default TokIconButton;